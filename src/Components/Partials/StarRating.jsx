import React, { useState } from "react";
import { FaStar } from "react-icons/fa";

const StarRating = () => {
  return (
	<>
	{[...Array(5).map(star => {
		
	})]}
	</>
  )
};

export default StarRating;
